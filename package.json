{
  "name": "nodejs-filesystem",
  "version": "1.0.0",
  "description": "api endpoint to create and retrieve text files",
  "main": "app.js",
  "scripts": {
    "start":"nodemon app.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "node",
    "express",
    "date-time"
  ],
  "author": "Sandy",
  "license": "MIT",
  "dependencies": {
    "express": "^4.21.1"
  },
  "devDependencies": {
    "nodemon": "^3.1.7"
  }
}
